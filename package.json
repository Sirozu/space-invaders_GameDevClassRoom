{
  "name": "space-invaders",
  "version": "1.0.0",
  "description": "",
  "main": "game.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "predeploy": "rm -rf dist && parcel build index.html --private--url /space-invaders-Sirozu",
    "deploy": "gh-pages -d dist"
  },
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "gh-pages": "^3.1.0",
    "parcel": "^1.12.4"
  }
}
